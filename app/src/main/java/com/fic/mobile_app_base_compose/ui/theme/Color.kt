package com.fic.mobile_app_base_compose.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta FitMatch ──────────────────────────────────────────────
// Rojo fuerte — color primario de la marca
val FitmachRed       = Color(0xFFD32F2F)   // Rojo intenso
val FitmachRedDark   = Color(0xFF9A0007)   // Rojo oscuro (dark mode)
val FitmachRedLight  = Color(0xFFFF6659)   // Rojo claro (hover / ripple)

// Gris metálico — secundario y superficies
val MetalGrey        = Color(0xFF546E7A)   // Gris azulado metálico
val MetalGreyDark    = Color(0xFF29434E)   // Gris oscuro (dark mode)
val MetalGreyLight   = Color(0xFF819CA9)   // Gris claro

// Fondos y superficies
val BackgroundLight  = Color(0xFFF5F5F5)   // Fondo claro casi blanco
val BackgroundDark   = Color(0xFF121212)   // Fondo oscuro Material
val SurfaceLight     = Color(0xFFFFFFFF)   // Superficie blanca
val SurfaceDark      = Color(0xFF1E1E1E)   // Superficie oscura

// Texto sobre colores primarios
val OnPrimary        = Color(0xFFFFFFFF)   // Texto sobre rojo
val OnSecondary      = Color(0xFFFFFFFF)   // Texto sobre gris